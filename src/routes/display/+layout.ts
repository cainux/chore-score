// Nothing on this page may render after the screenshot fires, so there is
// nothing for the client runtime to do once the HTML has arrived.
export const csr = false;
export const prerender = false;
