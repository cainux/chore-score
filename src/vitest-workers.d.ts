// Type declarations for the `cloudflare:test` module used by `*.workers.spec.ts`.
// The pool provides them, but only under an explicit reference.
/// <reference types="@cloudflare/vitest-pool-workers/types" />
