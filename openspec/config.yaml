schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# Per-operation guidance (optional)
# Add advisory guidance for how apply and archive work should be conducted.
# This is separate from artifact rules above.
# Example:
#   operations:
#     apply:
#       guidance:
#         - Keep test summaries concise
#     archive:
#       guidance:
#         - Summarize the archive outcome before finishing

context: |
  Dates and times are always UTC. Store, transport, log, and compute in UTC —
  never local times, offsets, or naive timestamps. Calendar dates are zone-free
  'YYYY-MM-DD' strings and date arithmetic is done in UTC so daylight saving
  transitions cannot change an interval's length.

  The sole exception is resolving the current Europe/London calendar date and
  clock time for the household — the date to decide which day it is, the time
  only so the wall display can state when it was rendered. Both must come from
  a single conversion in one function, so the zone is named exactly once.

rules:
  design:
    - Use ASCII or Mermaid diagrams where appropriate to best explain the
      design to readers
